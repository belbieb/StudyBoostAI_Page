document.addEventListener('DOMContentLoaded',()=>{const enhancement=document.createElement('link');enhancement.rel='stylesheet';enhancement.href='site-enhancements.css';document.head.appendChild(enhancement);const toggle=document.querySelector('.menu-toggle');const menu=document.querySelector('.nav-links');if(toggle&&menu){toggle.addEventListener('click',()=>{const open=menu.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));});menu.addEventListener('click',e=>{if(e.target.matches('a')){menu.classList.remove('open');toggle.setAttribute('aria-expanded','false');}})}const list=document.querySelector('.quote-list');if(list){const intro=document.createElement('div');intro.className='quote-intro';intro.innerHTML='<p class="eyebrow">WORDS TO KEEP CLOSE <span></span></p><h2 id="quote-heading">Three reminders for the hard days.</h2><p>These are a starting point. In the future, contribute a motivational quote of your own and help another student keep going.</p>';list.parentElement.prepend(intro);list.querySelectorAll('.quote-card').forEach((card,index)=>{const button=document.createElement('button');button.className='train-button';button.type='button';button.setAttribute('aria-label',`Train AI with quote ${index+1}`);button.textContent='✦ Train AI';card.append(button);});}const form=document.querySelector('#quote-form');if(form&&list){form.addEventListener('submit',e=>{e.preventDefault();const name=form.elements.name.value.trim();const quote=form.elements.quote.value.trim();if(!name||!quote)return;const card=document.createElement('article');card.className='quote-card';const p=document.createElement('p');p.textContent=`“${quote}”`;const cite=document.createElement('cite');cite.textContent=`— ${name}`;const button=document.createElement('button');button.className='train-button';button.type='button';button.textContent='✦ Train AI';card.append(p,cite,button);list.prepend(card);form.reset();document.querySelector('.form-status').textContent='Your quote has been added to the wall. Keep going!';})}});